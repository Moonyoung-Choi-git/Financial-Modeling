{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "prisma generate && next build",
  "framework": "nextjs",
  "regions": ["icn1"],
  "functions": {
    "app/**/*.ts": {
      "maxDuration": 10
    }
  }
}
